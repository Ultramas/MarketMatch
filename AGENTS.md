# AGENTS.md

## Repo Shape
- Firefox-first marketplace comparison extension scaffold.
- Current code is a copy of the browser-extension skeleton; live scraping/adapters are still placeholders.
- Keep consent/history logging and coupon support opt-in and user-driven.

## Commands
- No repo-standard install/test commands are established yet.
- Use the project README and `manifest.json` as the source of truth for extension behavior.

## Notes
- Preserve the scaffold structure and file contents unless the task explicitly changes them.
- Expect future work around marketplace adapters, normalization, ranking, consent, history, and coupon hooks.
